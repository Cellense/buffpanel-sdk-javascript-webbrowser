{
  "name": "buffpanel-sdk-javascript-webbrowser",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "^8.1.4",
    "npm": "^5.3.0"
  },
  "scripts": {
    "build": "uglifyjs --compress --mangle -- src/sdk.js > dist/web_sdk/sdk.js",
    "deploy": "git checkout . && git checkout master && git pull && npm i && mkdir -p dist/web_sdk && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Cellense/buffpanel-sdk-javascript-webbrowser.git"
  },
  "author": "Osama Hassanein <osama.hassanein@cellense.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Cellense/buffpanel-sdk-javascript-webbrowser/issues"
  },
  "homepage": "https://github.com/Cellense/buffpanel-sdk-javascript-webbrowser#readme",
  "devDependencies": {
    "uglify-js": "^3.0.3"
  }
}
